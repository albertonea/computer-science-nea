import static org.junit.jupiter.api.Assertions.assertTrue;

public class MatchingEngineTest {
//    @Test()
//    @DisplayName("when initialising a new matching engine then the order book should be empty")
//    void testInitialiseMatchingEngine() {
//        MatchingEngine matchingEngine = new MatchingEngine("AAPL");
//        assertTrue(matchingEngine.getOrderBook().isPresent());
//        assertTrue(matchingEngine.getOrderBook().get().getBuySide().isEmpty());
//        assertTrue(matchingEngine.getOrderBook().get().getSellSide().isEmpty());
//    }
//
//    @Test()
//    @DisplayName("given an empty orderbook when placing a buy order then the buy side contains the order")
//    void testPlaceBuyOrder(){
//        MatchingEngine matchingEngine = new MatchingEngine("AAPL");
//        matchingEngine.placeOrder(new BuyOrder(132, 31));
//    }
}
